using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class testWords : MonoBehaviour {

    controller mycontroller;
    allWords myword;
    // Use this for initialization
    void Start()
    {
        /*
        mycontroller = controller.Instance;
        myword = allWords.Instance;
        
        word AngryWord = new word("Angry", "frrrrr", "spppp");
        word AfterWord = new word("After", "frrrrr", "spppp");
        word AppleWord = new word("Apple", "frrrrr", "spppp");
        myword.add(AngryWord);
        myword.add(AfterWord);
        myword.add(AppleWord);

        mycontroller.orignalWords();
        //test sort
        mycontroller.sortWords();


        //test random
        word nowRandom = mycontroller.randomOneWord();
        print(nowRandom.getSpelling());

        //test favorite
        mycontroller.changeFavorite();

        mycontroller.changeFavorite();

        mycontroller.changeFavorite();
        */
    }
    public void prient(int b)
    {
        print(b);
    }

}
